<h1>Cart#index</h1>
<p>Find me in app/views/cart/index.html.erb</p>

<% if @cart.empty? %>
<p>You have not added any items yet</p>
<% end %>

<p id="notice"><%= notice %></p>
<div id="itemsContainer">
    <table id="customers">
        <% total = 0 %>
        <% @cart.each do |id| %>
            <% booking = Booking.find_by_id(id) %>
            <tr>
                <td class="title"><%= link_to booking.court_name, booking %></td>
                <a href ="/cart/remove/<%=item.id%>">Remove from cart</a></td>
                <td class="title"><%= link_to booking.start_time, booking %></td>
                <td class="quantity"><%= link_to booking.duratione, booking %></td>
                <td class="price"><%= number_to_currency(5 * booking.duration, :unit =>"€") %></td>
            </tr>
            <% total += 5 * booking.duration %>
        <% end %>
        <tr>
            Grang Total: <%= number_to_currency(total, :unit => "€") %>
        </tr>
    </table>
</div>